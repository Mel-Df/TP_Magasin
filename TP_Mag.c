#include <string.h>
#include <stdio.h>

struct Article {
    char *nomarticle;
    float prix;
    float quantité;   
};

typedef struct Article occulte; 
typedef struct Article etrange;

void paiemenent ( *bourse,){
float bourse;

bourse = 40;

printf ("Vous possedez %d pieces", bourse);
}

void constitution (*article){
 article occulte = {"Calice", 5, 1};
 article occulte = {"Fiole noire", 2, 1};
 article etrange = {"Doigt de troll", 10, 1};
 article etrange = {"Oeil", 15, 1};
};


void afficherarticles (*article) {
    printf ("Jetez un oeil ! Litteralement... \n");
    printf ("%d",  )
};



void main (){
  //strcpy(destination,source);

return 0;
}